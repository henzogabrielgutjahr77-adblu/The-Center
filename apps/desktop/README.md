# @the-center/desktop

Cliente Windows do The Center.

> **NÃO IMPLEMENTADO**: Este diretório existe apenas para manter a estrutura do
> monorepo. A implementação do cliente Windows será desenvolvida por outro
> OpenCode em outra máquina, dependendo do contrato em `packages/api-types` e
> `docs/API.md`.
